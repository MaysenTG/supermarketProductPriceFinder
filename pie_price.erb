<!DOCTYPE html>
<html>
<head>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f4f4f4;
    }
    .email-container {
      width: 100%;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
      background-color: #fff;
      box-shadow: 0px 0px 10px 0px rgba(0,0,0,0.1);
    }
    .email-header {
      text-align: center;
      padding-bottom: 20px;
      border-bottom: 1px solid #ddd;
    }
    .email-body {
      font-size: 24px;
      padding: 20px 0;
      text-align: center;
    }
  </style>
</head>
<body>
  <% product_url = "https://www.countdown.co.nz/shop/productdetails?stockcode=#{@product_stock_code}" %>
  <% formatted_pie_price = sprintf("$%.2f", @pie_price) %>
  <% formatted_price_goal = sprintf("$%.2f", @price_goal) %>

  <div class="email-container">
    <div class="email-header">
      <h2>Big Ben Mince and Cheese pies (6pk)</h2>
      <a href="<%= product_url %>" target="_blank">Link to Product</a>
    </div>
    </div>
    <div class="email-body">
      <% if @pie_price < @price_goal %>
        <p>yay price is <%= formatted_pie_price %> and under goal price (<%= formatted_price_goal %>)</p>
      <% else %>
        <p>Pies cost too much (costs <%= formatted_pie_price %>) :(</p>
      <% end %>
    </div>
  </div>
</body>
</html>
